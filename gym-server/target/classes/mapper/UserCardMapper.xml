<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.gym.mapper.UserCardMapper">

    <select id="selectCurrentCardByUserId" resultType="com.example.gym.entity.UserCard">
        SELECT * FROM user_card 
        WHERE user_id = #{userId} 
          AND status = 1 
          AND expire_time > NOW()
        ORDER BY expire_time DESC
        LIMIT 1
    </select>

    <select id="selectByUserId" resultType="com.example.gym.entity.UserCard">
        SELECT * FROM user_card 
        WHERE user_id = #{userId}
        ORDER BY purchase_time DESC
    </select>

</mapper>
